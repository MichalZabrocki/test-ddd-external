overblog_graphql:
    definitions:
        auto_compile: true
        schema:
            default:
                query: Query
                mutation: Mutation
        mappings:
            types:
                -
                    type: graphql
                    dir: "%kernel.project_dir%/config/graphql/types"
                    suffix: ~
    security:
        handle_cors: true
