overblog_graphql:
    definitions:
        auto_compile: false
        schema:
            default:
                query: Query
        mappings:
            types:
                -
                    type: graphql
                    dir: "%kernel.project_dir%/config/graphql/types"
                    suffix: ~
    security:
        handle_cors: true
